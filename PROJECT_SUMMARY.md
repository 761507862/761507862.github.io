# Aion Revenue Recorder (永恒之塔2 收益记录器) 项目总结

## 1. 项目简介
本项目是一个专为《永恒之塔2》玩家设计的 Web 应用程序，旨在帮助玩家高效管理多角色的游戏收益、资源消耗及副本进度。通过直观的数据看板和便捷的录入系统，玩家可以轻松追踪账号维度的经济状况和周常任务完成度。

## 2. 核心功能

### 📊 账号总览 (Account Dashboard)
*   **实时数据统计**：汇总显示账号下所有角色的基纳 (Kinah) 总收益与总消耗。
*   **实际收益计算**：支持自定义基纳比例，实时计算折合后的实际价值。
*   **副本进度追踪**：统计每周“远征队”和“超越之力”副本的通关次数。
*   **收益递减预警**：根据通关次数自动判断并展示当前的收益递减状态（正常/递减/严重递减）。

### 🦸 角色管理 (Character Management)
*   **角色卡片**：卡片式展示角色信息，包括职业、装分、奥德能量及溢出能量状态。
*   **周常任务追踪**：
    *   能量球购买与制作进度。
    *   关键副本（远征队、超越之力）及系统（觉醒、宠物）的周常次数统计。
*   **数据可视化**：通过进度条和徽章直观展示各项指标完成度。

### 💶 收益录入系统 (Revenue Entry)
*   **便捷弹窗录入**：通过全局导航栏快速唤起录入弹窗，无需跳转页面。
*   **智能数值处理**：
    *   输入逻辑优化：输入 `100` 自动识别为 `1,000,000` (100万)，大幅简化大额数值输入。
    *   强制非负校验：防止误操作输入负数。
*   **多角色支持**：在弹窗内可快速切换角色进行连续录入。

### 🛠️ 其他功能
*   **多服务器支持**：数据按服务器隔离，支持自定义服务器名称。
*   **数据管理**：提供 JSON 格式的数据导入与导出功能，保障数据安全。
*   **国际化 (i18n)**：完整支持中文/英文界面切换。
*   **主题切换**：支持明亮/暗黑模式。

## 3. 技术架构

*   **核心框架**：React 18 + TypeScript
*   **构建工具**：Vite
*   **状态管理**：Zustand (配合 Persist 中间件实现本地持久化)
*   **样式方案**：Tailwind CSS + Shadcn/ui (Radix UI)
*   **动画交互**：Framer Motion (实现平滑的数字滚动与转场效果)
*   **图标库**：Lucide React

## 4. 目录结构说明

```
src/
├── features/               # 业务功能模块
│   ├── account/            # 账号总览相关组件
│   ├── character/          # 角色管理相关组件
│   ├── dungeon/            # 副本记录相关逻辑
│   ├── items/              # 物品收益模块
│   ├── revenue/            # 收益录入模块 (Modal)
│   └── server/             # 服务器选择与管理
├── shared/                 # 共享资源
│   ├── components/         # 通用 UI 组件 (Button, Card, Dialog 等)
│   └── ui/                 # 基础原子组件
├── store/                  # Zustand 状态管理
│   ├── slices/             # 状态切片 (Account, Character)
│   └── useGameStore.ts     # Store 组合与导出
├── lib/                    # 工具函数
│   ├── dateUtils.ts        # 日期与时区处理 (Asia/Shanghai)
│   └── utils.ts            # 通用辅助函数 (格式化等)
├── config/                 # 全局配置常量 (游戏限制、倍率等)
└── i18n/                   # 国际化语言包
```

## 5. 最近更新日志 (Changelog)

### ✨ 新增功能
*   **收益录入重构**：将收益录入从独立页面重构为全局 Modal 弹窗，提升了交互流畅度。
*   **智能数字动画**：
    *   优化 `NumberTicker` 组件，实现“仅滚动末尾两位数字”的动画效果。
    *   强制数值显示为整数，消除小数显示，提升视觉整洁度。
*   **低饱和度图标**：重制了物品图标生成算法，采用低饱和度、非红色的配色方案，视觉更专业。

### 💄 UI/UX 优化
*   **布局调整**：将“收益录入”和“物品收入”按钮移动至对应模块标题栏右侧，优化页面空间利用。
*   **输入体验**：移除了输入框原本的上下增减按钮（Spinner），简化输入界面；移除了冗余的占位符提示。
*   **界面精简**：移除了主页面的副标题文本。

### 🔧 代码优化
*   **代码清理**：移除全项目未使用的变量、导入及 `console.log`。
*   **性能提升**：优化 `Intl.NumberFormat` 的实例化逻辑，减少渲染开销。
*   **日期统一**：封装 `getDisplayDateString` 方法，统一全站日期显示格式。

### 🚀 部署支持
*   **Cloudflare Pages**：新增 `public/_redirects` 配置文件，完美支持 Cloudflare Pages 的 SPA 路由模式，防止刷新页面 404。

